public class Metodos{
    public static void main(String[] args) throws Exception {
        System.out.println("Métodos");

        /* Como declarar:
            nomeado como verbo;
            seguir o padrão camelCase; 

            public double dividir(int dividendo, int divisor) throws Exception {           <- throws Exception indica que o método pode lançar uma exceção
                if (divisor == 0) {
                    throw new Exception("Divisão por zero não é permitida");
                }
                return (double) dividendo / divisor;
            }
            
            private void metodoPrivado() {  <- private indica que o método só pode ser acessado dentro da própria classe
                System.out.println("Método privado chamado");
            }

            void metodoSemRetorno() {  <- void indica que o método não retorna nenhum valor
                System.out.println("Método sem retorno chamado");
            }
         */

    }
}
